from talon import settings as settings
from typing import Sequence

def transform_samples(samples: Sequence[float], *, pad_left: bool, pad_right: bool) -> list[float]: ...

class masked_list(list): ...
