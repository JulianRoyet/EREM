from talon.api import ffi as ffi, ffi_string as ffi_string, lib as lib
from typing import Dict, Sequence

class Flac:
    samples: Sequence[float]
    sample_rate: int
    channels: int
    comments: Dict[str, str]
    def __init__(self, samples, sample_rate, channels, comments) -> None: ...

def read_file(path: str) -> Flac: ...
def write_file(path: str, samples: Sequence[float], *, sample_rate: int = ..., channels: int = ..., compression_level: int = ..., comments: Dict[str, str] = ...) -> None: ...
write_flac = write_file

def encode(samples: Sequence[float], *, sample_rate: int = ..., channels: int = ..., compression_level: int = ..., comments: Dict[str, str] = ...) -> bytes: ...
