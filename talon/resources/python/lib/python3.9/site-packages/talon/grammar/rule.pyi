from . import parser as parser
from .parser import Tree as Tree
from typing import Any, Callable, Optional

PathHook = Callable[[str], str]

class Rule:
    rule: str
    anchor: str
    path_hook: Optional[PathHook]
    root: Optional[parser.Node]
    ref: Any
    def __init__(self, rule: str, ref: Any, path_hook: PathHook = ...) -> None: ...
    def parse(self) -> None: ...
