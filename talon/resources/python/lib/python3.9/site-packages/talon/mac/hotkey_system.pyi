from .types import ScriptImpl as ScriptImpl
from talon import cron as cron, ctrl as ctrl, tap as tap
from talon.scripting import dispatch as dispatch
from talon.scripting.hotkey_system import BaseHotkeySystem as BaseHotkeySystem
from typing import Optional

class HotkeySystem(BaseHotkeySystem):
    tap_active: bool
    active_keys: dict[str, tap.KeyEvent]
    active_hotkeys: dict[str, tap.KeyEvent]
    repeating: dict[str, list['ScriptImpl']]
    job: Optional[cron.Job]
    def __init__(self) -> None: ...
    def run(self, impls: list['ScriptImpl']) -> None: ...
    def tick(self) -> None: ...
    def on_key(self, e: tap.KeyEvent) -> None: ...
    def update(self) -> None: ...
